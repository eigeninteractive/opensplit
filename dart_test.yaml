tags:
  # Needs a local Supabase instance (`supabase start`). The tests skip
  # themselves when nothing is listening, so a plain `flutter test` stays green
  # without it — but they are the only thing that can catch an adapter bug.
  integration:
    timeout: 2x
