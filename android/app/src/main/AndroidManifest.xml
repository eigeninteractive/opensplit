<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- enableOnBackInvokedCallback opts into Android 13+ predictive back, so
         dragging the back gesture shows a live preview of where it will land
         rather than a blank guess. Flutter's own
         PredictiveBackPageTransitionsBuilder is already the default Android
         page transition; without this manifest flag the framework never
         receives the gesture progress, so that animation never runs. -->
    <application
        android:enableOnBackInvokedCallback="true"
        android:label="OpenSplit"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- App Links. The same URL is a web route and a native route, so
                 an invite link tapped by someone without the app opens the web
                 build and just works — deferred deep linking for free, which
                 otherwise needs a paid SDK.

                 autoVerify makes Android fetch
                 /.well-known/assetlinks.json. If that file does not list the
                 certificate the installed app was actually signed with, this
                 fails silently and links open in a browser instead. See
                 web/.well-known/README.md.

                 One host, matching linkHost in lib/config.dart. Every host
                 listed here is a permanent commitment — it has to keep serving
                 assetlinks.json for as long as any link naming it exists, which
                 for a link pasted into a chat is indefinitely. A vanity domain
                 added later should redirect here rather than be claimed as a
                 second host. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="opensplit.web.app"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ hides other apps unless they are declared here. Without
             this, resolving a upi:// intent silently finds nothing and the
             settle-up handoff appears broken with no error. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="upi"/>
        </intent>
    </queries>
</manifest>
