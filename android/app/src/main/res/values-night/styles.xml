<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window while the process starts, on releases older than Android 12.
         @drawable/launch_background is written by
         `dart run flutter_native_splash:create`: the surface colour with the
         brand mark centred on it. Android 12 and up ignore this entirely and
         use values-night-v31/styles.xml instead. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- The window behind the Flutter UI once it is running. Visible during a
         rotation or a resize, so it is the app's surface rather than the
         platform's ?android:colorBackground, which is plain white. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/surface</item>
    </style>
</resources>
